lna (large number arithmetic)


Las operaciones mul y add operan sobre numeros de cualquier tamaño
y devuelven el resultado en hexadecimal.

Para generar los ejecutables en ./build:

$ make mul add

O para instalarlos en /usr/local/bin

$ sudo make install


Los test que están en el Makefile comparan con los resultados de la
operación utilizando la librería GMP. Por eso, para compilarlos
primero hay que instalar gmp.



