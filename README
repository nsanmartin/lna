lna (large number arithmetic)

El tipo struct digit representa los números naturales permitiendo
las operaciones de suma y producto entre números arbitrariamente
grandes y división de uno arbitrariamente grande por uno de precisión
fija.

Para probar la suma el producto y la división, por ejemplo:

make $OP
./build/$OP NUM NUM

donde $OP es add, mul o divdui.

TODO

Implementar la división para todo par de números naturales
arbitrariamente grandes (Cf. el libro de Knuth).

Implementar la operación que convierta una representación de un
número en una string hexadecimal, en una decimal.

Implementar el tipo lni_t (enteros) que permita realizar dichas
operaciones con números enteros. Esto puede hacerse mediante un tipo
que contenga un natural, su signo, y su número de dígitos (esto
para hacer más rápidas algunas operaciones, como por ejemplo algunas
comparaciones).

Implementar una calculadora. Podría hacerse con una pila de números,
que des-apile cada vez que se realice una operación. También podría
incluirse la posibilidad de programar funciones que no se limiten
a las operaciones básicas mencionadas, sino que consistan en la aplicación
sucesiva de operaciones básicas.
