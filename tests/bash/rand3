fst=$(($RANDOM % 20))
snd=$(($RANDOM % 20))
trd=$(($RANDOM % 20))


cat /dev/urandom | tr -cd '0-9' | head -c $fst
printf " "
cat /dev/urandom | tr -cd '0-9' | head -c $snd
printf " "
cat /dev/urandom | tr -cd '0-9' | head -c $trd
